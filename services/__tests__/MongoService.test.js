const assert = require('assert');
const MongoService = require('../mongoService.js');

describe('MongoService', () => {
  it('has a test', () => {
    assert(false, 'MongoService should have a test');
  });
});
