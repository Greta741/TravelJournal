const assert = require('assert');
const SitemapService = require('../sitemapService.js');

describe('SitemapService', () => {
  it('has a test', () => {
    assert(false, 'SitemapService should have a test');
  });
});
