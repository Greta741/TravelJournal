const assert = require('assert');
const HashService = require('../hashService.js');

describe('HashService', () => {
  it('has a test', () => {
    assert(false, 'HashService should have a test');
  });
});
