const assert = require('assert');
const DisplayService = require('../displayService.js');

describe('DisplayService', () => {
  it('has a test', () => {
    assert(false, 'DisplayService should have a test');
  });
});
