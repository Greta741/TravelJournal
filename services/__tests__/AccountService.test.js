const assert = require('assert');
const AccountService = require('../accountService.js');

describe('AccountService', () => {
  it('has a test', () => {
    assert(false, 'AccountService should have a test');
  });
});
