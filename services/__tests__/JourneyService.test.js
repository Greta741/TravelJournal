const assert = require('assert');
const JourneyService = require('../journeyService.js');

describe('JourneyService', () => {
  it('has a test', () => {
    assert(false, 'JourneyService should have a test');
  });
});
