const assert = require('assert');
const AccountDataService = require('../accountDataService.js');

describe('AccountDataService', () => {
  it('has a test', () => {
    assert(false, 'AccountDataService should have a test');
  });
});
